The code asks for basic inputs. 
Every parameter is an instance variable of the Genetic Algorithm class and can be changed directly from there.
The path of the file can be changed inside the runAlgorithm() inside GeneticAlgorithm class.  